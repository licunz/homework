#include <iostream>
#include<array>
#include<iomanip>

using namespace std;

int main()
{
   array<int,7>alphabet={0,1,2,3,4,5,6};
   array<int,5>grades;
   cin>>grades[4];
   array<int,5>values;
   
   for(int i=0;i<values.size();i++)
    n[i]=8;
    array<double,100>temperatures;
     int sum=0;
   for(int j=0;j<temperatures.size();j++)
  
       sum=temperatures[j]+sum;
        
    cout<<"sum is"<< sum;
    array<double,99>w;
     for(int i=0;i<w.size();i++)
    n[i]=i;
    int b=w[0];
    int s=w[0];
    for(int i=1,i<w.size();i++)
      {
          if(b<w[i])
            b=w[i];
        
      }  
    cout<<b;
    for(int i=1,i<w.size();i++)
      {
          if(s>w[i])
            s=w[i];
          
      }  
    cout<<s;
}
